# Java-OpenGL
Currently in development

# Try it out (very raw)
Checkout dev branch
Create enum value in en.opengl.com.params.Model with paths to your local .obj and texture files
For controls check en.opengl.com.Animator
